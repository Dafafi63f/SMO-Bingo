# Tests

Suite `unittest` (stdlib) para helpers y consistencia Combined ↔ `catalog/`.

```bash
python -m unittest discover -s tests -t . -v
```

En GitHub: `.github/workflows/tests.yml` (push/PR a `main`).
